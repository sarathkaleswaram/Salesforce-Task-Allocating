<apex:page controller="AccConrelclass">
    <apex:form >
        <apex:pageBlock >
            <apex:pageBlockTable value="{!conlist}" var="array" >;
                
                <apex:column value="{!array.Name}" />
                <apex:column value="{!array.Account.Name}"/>
                <apex:column value="{!array.Account.Industry}"/>
                
            </apex:pageBlockTable> 
        </apex:pageBlock>   
    </apex:form>
</apex:page>